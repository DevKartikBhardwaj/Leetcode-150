//i used hashmap to keep tarack of s to t character mapping 
//and used a array to keep track whether the t elems is 
//previously mapped or not before mapping them with s

class Solution {
    public boolean isIsomorphic(String s, String t) {
        Map<Character,Character>mp=new HashMap<>();
        char[] sArray=s.toCharArray();
        char[] tArray=t.toCharArray();

        int[] hash=new int[256];

        if(s.length()!=t.length())
            return false;
        
        for(int i=0;i<s.length();i++){
            char ch1=sArray[i];
            char ch2=tArray[i];
            if(mp.containsKey(ch1)){
                if(mp.get(ch1)!=ch2)return false;
                continue;
            }
            if(hash[ch2]==1)return false;
            hash[ch2]=1;
            mp.put(ch1,ch2);
        }
        return true;
    }
}
